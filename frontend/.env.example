# Frontend build-time configuration.
#
# In Docker and in local development the API is same-origin, so this can be
# left as "/api". Point it at an absolute URL only if the API is on another host.
VITE_API_URL=/api

# Backend the Vite dev server proxies to (development only).
VITE_API_PROXY=http://localhost:8000
