# Dependencies for the Vercel Python function (api/index.py).
#
# Kept in sync with backend/requirements.txt, minus the packages that only
# matter outside the serverless runtime:
#   uvicorn  - Vercel provides the ASGI server
#   alembic  - migrations are applied from a workstation/CI, not at cold start
fastapi==0.115.6
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
pydantic==2.10.4
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
argon2-cffi==23.1.0
python-multipart==0.0.20
email-validator==2.2.0
user-agents==2.2.0
